Задача 1:
	- метод CalcTriangleArea го преименуваме на CalculateTriangleArea. Аргументите на метода ги преименуваме на  
	  sideA, sideB, sideC, това е по-описателно, въпреки, че според мен и а, b, c също са ОК. Променливата s -> semiPerimeter.
	  Добавяме допълнителен метод IsPositiveNumber за да тестваме дали страните на триъгълника са положителни.
	  Ако имаме некоректен вход, т.е. някоя от страните на триъгълника е отрицателна хвърляме exception.
	  
	- променяме името на NumberToDigit -> ConvertNumberToString. Оправяме case-совете добавяме default.
	
	- променяме FindMax->FindMaxElementInArray. При празен array хвърляме изключение. Добавяме нова променлива 
	  maxElement, в която пазим максимума. По този начин не променяме входа, което не е добра практика. 
	  
	- метода PrintAsNumber го разделяме на три нови метода.
	
	- преименуваме CalcDistance -> CalculateDistance. Координатите може да ги преименуваме на x1 -> coordX1, y1 -> coordY1...
      Аз лично ще ги оставя не променени, защото го намирам за по-четимо. isHoriyontal и isVertical ще ги извадя в нови методи,
      защото един метод трябва да прави това, което казва името му и ако може то да е едно нещо.	  