Задача 1:
	Основни промени, които съм направил:
		- прекръстих основния файл на Event.cs, защото според правилата името на класа и името на файла е хубаво да съвпадат. Името на файла трябва да е с главна буква. 
		  Според слайдовете: File containing a class Student should be names Student.cs 
		- ръчно пооправих всичко, което на пръв поглед изглеждаше не добре форматирано, т.е. липсващи скоби след if-ове, команди на един ред ги разделих, изтрих празните редове и т.н.
		- добавям референция към Wintellect.PowerCollections.
		- стартирам StyleCop. При мен имаше около 50 предупреждения, които отстраних.   
			- class Event го направих public
			- в class Event полетата date, title, location ги направих private и им добавих пропъртита, които са public. 
			   Понеже пропъртитата са автоматични, може и без полета, но на мен така повече ми харесва, а и е по-лесно за промяна, ако решим да добавим валидации
			- навсякъде където сме използвали полетата ги заменяме с пропъртитатата.
			- там където дефинираме променлива String е по-добре да използваме string.
			- в Event -> CompareTo имаме променливи byDate, byTitle, byLocation, което не е добро именуване. Променам ги на comparedByDate, comparedByTitle и comparedByLocation, 
			  защото те съдържат резултата от сравнението.
			- използването на "" за празен string не е добра практика. По-добре е да използваме string.Empty.
			- навсякъде съм добавил липсващите access modifier. За подбора не съм внимавал много. В началото сложих всичко private  и само където се счупи съм го променял.
		- Понеже не е добра практика да имаме много класове в един файл, съм отделил всеки клас в отделен файл.
		- в Event.cs -> ToString името на променливата от тип StringBuilder е toString, което е крайно не подходящо. Преименувам я на result, което е често срещано.
		- в Event.cs -> ToString стринга "yyyy-MM-ddTHH:mm:ss" е добре да се изнесе като константа.
		- добра практика е да се замества "\n" c Environment.NewLine. Така не си зависим от платвормата. на която ще работи програмата.
			  

Задача 2:
	Основни промени, които съм направил:

		- първо ръчно пооправих кода и използвах beautify-ер за да получа по-четливи неща.
		- после следвах инструкциите на JSHint и попълних липсващите ';' и var-рове.
		- сложих всичко в едно IIFE и добавих 'use strict'
		- преместих декларациите на всички променливи най-отгоре, като оставих инициализацията им максимално близко до кода, където са използвани.
		- премахнах излишните променливи, т.е. тези, които никъде не се използват*(txt, off, args)
		- по препоръка на JSHint document.layers['ToolTip'].visibility го замених с  document.layers.ToolTip.visibility, т.е. той-препоръчва да се използва точкова нотация
		- имената на функциите, които бяха с PascalCase ги промених на camelCase
		- тук могат да се променят част от имената на променливите. pX и pY не са толкова описателни. По-добре е да се променят на positionX и positionY.
		  Променливата също не е много описателна.
		- махнал съм eval, като не съм сигурен дали това е най-правилния начин.
		- заменил съм '==' с '==='
		- хубаво е навсякъде да се използват едни и същи кавички, т.е. може да са двойни, може да са единични, но да са еднакви навсякъде. Аз предпочитам единични.
		- променяме кода на функцията mouseMove, защото правим два пъти една и съща проверка.
		- може би трябва да се променят имената на функциите showMenu1, showMenu2, hideMenu1, hideMenu2 с нещо по-добро, но аз не съм навлизал толкова в детайли